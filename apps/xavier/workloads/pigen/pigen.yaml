apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: pigen
  namespace: argocd
spec:
  destination:
    namespace: pigen
    server: 'https://kubernetes.default.svc'
  project: default
  sources:
    - repoURL: 'https://github.com/dipa-ai/pigen.git'
      targetRevision: HEAD
      path: kubernetes/
      kustomize:
        images:
          - 'pigen=registry.luxembourg-2.cloud.gcore.dev/59508-197526-76-infra/pigen:v1.0.0'
        patches:
          - target:
              kind: Deployment
              name: pigen
            patch: |-
              - op: add
                path: /spec/template/spec/imagePullSecrets
                value:
                  - name: gcore-registry-secret
          - target:
              group: gateway.networking.k8s.io
              version: v1
              kind: HTTPRoute
              name: pigen
            patch: |-
              - op: replace
                path: /spec/hostnames
                value: ['pigen.apten.im']
              - op: replace
                path: /spec/parentRefs/0/name
                value: envoy-gateway-external
              - op: replace
                path: /spec/parentRefs/0/sectionName
                value: https-pigen
          - target:
              group: gateway.networking.k8s.io
              version: v1alpha2
              kind: TCPRoute
              name: pigen-pi
            patch: |-
              - op: replace
                path: /spec/parentRefs/0/name
                value: envoy-gateway-external
              - op: replace
                path: /spec/parentRefs/0/sectionName
                value: pigen-tcp
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true